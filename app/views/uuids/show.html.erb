<p id="notice"><%= notice %></p>

<p>
  <strong>ID:</strong>
  <%= @uuid.id %>
</p>

<p>
  <strong>Unpacked:</strong>
  <%= @uuid.unpacked %>
</p>

<%= link_to 'Edit', edit_uuid_path(@uuid) %> |
<%= link_to 'Back', uuids_path %>
