<%= form_with(model: marc_file, local: true) do |form| %>
  <% if marc_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(marc_file.errors.count, "error") %> prohibited this marc_file from being saved:</h2>

      <ul>
      <% marc_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :folder %>
    <%= form.text_field :folder %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :checksum %>
    <%= form.text_field :checksum %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
