<%= form_with(model: umpebc_marc, local: true) do |form| %>
  <% if umpebc_marc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(umpebc_marc.errors.count, "error") %> prohibited this umpebc_marc from being saved:</h2>

      <ul>
      <% umpebc_marc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :doi %>
    <%= form.text_field :doi %>
  </div>

  <div class="field">
    <%= form.label :mrc %>
    <%= form.text_field :mrc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
