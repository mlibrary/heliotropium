<%= form_with(model: kbart_file, local: true) do |form| %>
  <% if kbart_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kbart_file.errors.count, "error") %> prohibited this kbart_file from being saved:</h2>

      <ul>
      <% kbart_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :folder %>
    <%= form.text_field :folder %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :updated %>
    <%= form.date_select :updated %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
